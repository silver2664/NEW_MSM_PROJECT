<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="orderMapper">

<!-- 주문 -->
<insert id="orderInsert">
       INSERT INTO MORDER (ORDERNUM,ORDERPRODUCTNAME,ORDERAMOUNT,ORDERPRICE,TOTALMONEY,ORDERNAME,ORDERPHONE,ORDERADDR1,ORDERADDR2,ORDERADDR3,ORDERREQ,MID,MGNUM)
       VALUES (order_seq.nextval,#{orderProductName},#{orderAmount},#{orderPrice},#{totalMoney},#{orderName},#{orderPhone},#{orderAddr1},#{orderAddr2},#{orderAddr3},#{orderReq},#{mId},#{mgNum})
</insert>

<select id = "orderList" resultType = "com.project.order.vo.OrderVO">
	<![CDATA[
		SELECT
			ORDERPRODUCTNAME,
			ORDERAMOUNT,
			ORDERPRICE,
			ORDERSTATE
		FROM
			MORDER
		WHERE MID = #{mId}
	]]>
</select>


</mapper>